# ==================== Docker Compose 环境变量配置 ====================

# ========== 端口配置 ==========
# 前端 Web 服务端口
MUPLAYER_PORT=25884

# 后端 API 服务端口
# 格式：IP:端口 或 端口
# 127.0.0.1:3000 - 仅本地访问（推荐，前端容器通过内部网络访问）
# 3000 或 0.0.0.0:3000 - 允许外部访问（独立部署后端时使用）
MUSIC_API_PORT=127.0.0.1:3000

# ========== 后端 API 配置 ==========
# 日志级别：debug, info, warn, error
LOG_LEVEL=info

# CORS 允许的来源（容器部署时通常不需要配置）
# 如果前后端分别部署，需要设置为前端地址
CORS_ALLOW_ORIGIN=http://localhost:25884

